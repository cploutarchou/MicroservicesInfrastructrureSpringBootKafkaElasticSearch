spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cploutarchou/microservices-app-config-repository
          default-label: master
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}
          clone-on-start: true